# 버전 고정(pin): Streamlit Cloud가 리부트마다 최신 패키지를 새로 설치해
# 예측 불가한 조합으로 부팅 실패("Oh no. Error running app.")하는 것을 방지.
# 아래는 로컬에서 앱 정상 구동을 확인한 조합. 업그레이드는 로컬 검증 후 수동으로.
streamlit==1.57.0
requests==2.32.5
pandas==3.0.1
numpy==2.4.2
openpyxl==3.1.5
plotly==6.7.0
google-api-python-client==2.196.0
google-auth==2.48.0
duckdb==1.5.5
pyarrow==24.0.0
pytest==9.1.1
